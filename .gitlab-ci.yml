image: node:16.17.0

before_script:
  - npm config set registry https://registry.npmmirror.com
  - yarn install --immutable

cache:
  paths:
    - node_modules

stages:
  - build

build_job:
  stage: build
  script:
    - yarn run build
